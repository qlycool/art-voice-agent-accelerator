# ---------------------------------------------------------------------
# RTInsuranceAgent – Authentication Task Agent
# ---------------------------------------------------------------------

agent:
  name: AuthAgent
  creator: Pablo Salvador
  organization: GBB AI
  description: |
    Handles caller authentication, then hands off to clinical agents.

model:
  deployment_id: gpt-4o
  temperature: 0.15
  top_p: 0.9
  max_tokens: 1024

prompts:
  path: voice_agent_authetication.jinja

tools:
  - authenticate_caller
  - escalate_emergency