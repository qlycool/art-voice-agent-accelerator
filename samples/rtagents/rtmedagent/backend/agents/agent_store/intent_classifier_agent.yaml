agent:
  name: IntentClassifierAgent
  creator: Pablo Salvador
  organization: GBB AI
  description: |
    Classifies incoming user requests into domain-specific categories
    (Medication, Billing, Demographics, Referrals, General Healthcare, Non-Healthcare, Fallback).
    Does not answer any service question directly.

model:
  deployment_id: gpt-4.1-nano
  temperature: 0
  top_p: 1
  max_tokens: 100

prompts:
  path: intent_classifier_agent.jinja

tools: []
