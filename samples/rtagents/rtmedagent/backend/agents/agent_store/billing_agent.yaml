# ---------------------------------------------------------------------
# RTMedAgent – Billing Agent
# ---------------------------------------------------------------------

agent:
  name: BillingAgent
  creator: Pablo Salvador
  organization: GBB AI
  description: |
    Voice agent specializing in billing and insurance for XYZ Health Clinic.
    Handles payment, coverage, and insurance questions after multi-factor authentication.
    Strictly uses tool-calling—never generates billing advice or answers outside its function scope.

model:
  deployment_id: gpt-4o-standard
  temperature: 0.15
  top_p: 0.9
  max_tokens: 1024

prompts:
  path: billing_agent.jinja

tools:
  - insurance_billing_question
  - escalate_emergency
